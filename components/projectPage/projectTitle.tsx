export default function ProjectTitle({ title }: any) {
  return (
    <div className="pt-[50px] text-[50px] text-pink text-bold">
      <p className="flex w-screen justify-center items-center">{title}</p>
    </div>
  )
}
